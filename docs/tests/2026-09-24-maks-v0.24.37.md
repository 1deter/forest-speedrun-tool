# Test list for maks - v0.24.37 (2026-09-24)

**Drafted, not yet sent** - the author sends it (or edits it first; keep
this file verbatim to what was sent). When the author pastes maks's
answers, match them to these numbers.

```
ForestOverlay v0.24.37 - Megan tests (update in game first: Updates tab)

1) Take a savestate during Megan's transformation (late, like your usual one), fight or kill her, then Quick load it. Megan should sit down again, the cutscene should replay fast to your moment, and no boss, body or babies should be left behind.
2) Same as 1, but press Quick load while the cutscene is still playing.
3) Listen after the Quick load: do the transformation sounds and music line up with the picture, as in a normal run? Nothing should keep playing into the fight.
4) Load your save from the main menu and Quick load the mid-cutscene savestate straight away (don't walk around first). The cutscene should play normally, with your weapon back at its end.
5) Throw some spears and miss, then Quick load. The thrown spears should be gone, and your inventory should have them.
6) Afterwards, send LogOutput.log (BepInEx folder).
```

What each item checks:

1. `Game/MeganKeeper` rebuild (v0.24.35). Log: the restore line's
   `megan: seated Megan put back (removed ...)`, then the fast-forward
   line. His old files (no `megan` line) take the bare-`seated` path.
2. The mid-cutscene path: `the running transformation stopped` in the
   `megan:` note (the game's `unlockPlayerParams`).
3. `Game/CutsceneAudio` (v0.24.36) - his original report ("transformation
   audio late, into the fight"). The fast-forward line ends with
   `sounds: N started during it - k moved to their place (...)`. Only he
   knows the right sound; the author does not.
4. The v0.24.37 fix: `megan: seated before the restore, left to the
   game`. Weapon: `held before it: 1 of 1 slot(s) set back`.
5. `removed N spear(s) thrown or dropped since the capture` (v0.24.36).
