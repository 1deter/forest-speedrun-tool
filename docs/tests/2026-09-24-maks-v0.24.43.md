# Test list for maks - v0.24.43 (2026-09-24)

**Drafted, not yet sent** - the author sends it (or edits it first; keep
this file verbatim to what was sent). When the author pastes maks's
answers, match them to these numbers.

```
ForestOverlay v0.24.43 - Megan and endgame tests (update in game first: Updates tab)

1) Take a savestate during Megan's transformation (late, like your usual one), fight or kill her, then Quick load it. Megan should sit down again, the cutscene should replay fast to your moment, and no boss, body or babies should be left behind.
2) Same as 1, but press Quick load while the cutscene is still playing.
3) Listen after the Quick load: do the transformation sounds and music line up with the picture, as in a normal run? Nothing should keep playing into the fight.
4) Load your save from the main menu and Quick load the mid-cutscene savestate straight away (don't walk around first). The cutscene should play normally, with your weapon back at its end.
5) Throw some spears and miss, then Quick load. The thrown spears should be gone, and your inventory should have them.
6) Practice tab, a spot with a start state: is the new Quick load / Full load switch clear? Does Restart (Quick) / Restart (Full) do what it says?
7) Cave 5: take the first coin pile by the drop, then take a savestate and Full load it. The coins should stay gone (also a few seconds later).
8) Red elevator: take a NEW savestate in front of it (older ones can't do this), ride it up, then Quick load. The elevator should be back down and ridable again, and the hallway should look as it did when you took the savestate (invisible if it was).
9) After riding the red elevator up, use Go to a spot outside the endgame (e.g. the vault door cave, or outside the Sahara cave). Both should look normal (the Sahara cave's outside visible).
10) Full load a savestate where you hold a weapon and the lighter. You should be able to swing and the lighter should give light straight away.
11) Afterwards, send LogOutput.log (BepInEx folder).
```

What each item checks:

1. `Game/MeganKeeper` rebuild (v0.24.35). Log: the restore line's
   `megan: seated Megan put back (removed ...)`, then the fast-forward
   line. His old files (no `megan` line) take the bare-`seated` path.
2. The mid-cutscene path: `the running transformation stopped` in the
   `megan:` note (the game's `unlockPlayerParams`).
3. `Game/CutsceneAudio` (v0.24.36) - his original report ("transformation
   audio late, into the fight"). The fast-forward line ends with
   `sounds: N started during it - k moved to their place (...)`. maks
   runs it currently and knows the exact audio; the author has not
   played in a while.
4. The v0.24.37 fix: `megan: seated before the restore, left to the
   game`. Weapon: `held before it: 1 of 1 slot(s) set back`.
5. `removed N spear(s) thrown or dropped since the capture` (v0.24.36).
6. The v0.24.38 switch (his v0.24.29 item: the old checkbox was unclear).
   Log: `Restart '<id>': restoring its start state in place.` (Quick) or `with a load.` (Full).
7. v0.24.39 late pickup removal: `removed N that appeared late`; if the
   coins stay, the first pass's `Not at capture but kept: ...` says why.
8. v0.24.40-41 (`ElevatorKeeper`, `AreaKeeper`; confirmed by the author).
   Log: `elevators: 1 put back` and `area: left '...' (none active at capture)`.
   His original report: elevator left at the overlook, Sahara half-loaded,
   endgame cave visuals gone.
9. v0.24.42 `AreaKeeper.ForTeleport`: the `Teleport to` line says
   `area: left '...', overlook flag cleared (outside the endgame sections)`.
   A spot within 2 m of a section's geometry counts as inside (no line).
10. v0.24.43 `RefreshHeld`: `held at capture, equipped again for the animator: ...`.
