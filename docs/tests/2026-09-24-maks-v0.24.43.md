# Test list for maks - v0.24.43 (2026-09-24)

**Drafted, not yet sent** - the author sends it (or edits it first; keep
this file verbatim to what was sent). When the author pastes maks's
answers, match them to these numbers.

Left out because the author confirmed them in game (see CLAUDE.md
*Confirmed in game*): Megan after a Quick load (before / during / after
her transformation, straight after a load from the title screen, the
weapon back), thrown spears removed, the red elevator after a Quick load,
Go out of the endgame after the ride, held items after a Full load.

```
ForestOverlay v0.24.43 - tests (update in game first: Updates tab)

1) Megan: take a savestate during her transformation (late, like your usual one), fight or kill her, then Quick load it. Listen: do the transformation sounds and music line up with the picture, as in a normal run? Nothing should keep playing into the fight.
2) Practice tab, a spot with a start state: is the new Quick load / Full load switch clear? Does Restart (Quick) / Restart (Full) do what it says?
3) Cave 5: take the first coin pile by the drop, then take a savestate and Full load it. The coins should stay gone (also a few seconds later).
4) Afterwards, send LogOutput.log (BepInEx folder).
```

What each item checks:

1. `Game/CutsceneAudio` (v0.24.36) - his original report ("transformation
   audio late, into the fight"). The fast-forward line ends with
   `sounds: N started during it - k moved to their place (...)`. Kept
   although the author heard it as "sounded perfect": maks runs it
   currently and knows the exact audio; the author has not played in a
   while.
2. The v0.24.38 switch (his v0.24.29 item: the old checkbox was unclear).
   Log: `Restart '<id>': restoring its start state in place.` (Quick) or
   `with a load.` (Full).
3. v0.24.39 late pickup removal: `removed N that appeared late`; if the
   coins stay, the first pass's `Not at capture but kept: ...` says why.
