# maks - red elevator retest (v0.24.66, 2026-09-25)

Posted by the bot in the QA Discord's #general (author approved the text),
as a reply to maks's forwarded report: a restart during the red elevator
ride "works half the time" - the elevator gone or the textures unloaded /
altered, sometimes 3-7 s later (his log: v0.24.52, every failure
`elevators: ... 1 left moving`). Fixed in v0.24.64 (`ElevatorKeeper.StopRide`).
He must update from v0.24.52 first.

Sent verbatim:

```
1) Start the red elevator ride, restart during the keycard animation. Is the car still there and can you ride it again?
2) Same, but restart once the car is moving.
3) Do the textures look right after both?
```

What each checks:

1. A restart inside the ride's 5 s keycard wait (before `Goto` moves the
   car): the ride stopped, car back, `_useCount` back - ridable again.
   Log: `elevators: 1 put back, ... 1 ride(s) stopped`.
2. A restart after the car and player were moved up (during the 25 s
   `_duration` wait): same, plus the area switch undone
   (`area: left 'Sections/HellCorridor'`).
3. The part the bridge could not judge (a `tp` into the endgame lands with
   the sections unloaded): the look after a restart on foot. If wrong, ask
   for the restore's `Savestate areas after the restore:` line.
