# QA list - v0.24.98 to v0.24.100, 2026-09-26

Posted in #general by the bot (whole team), with a summary of what changed
(message `1553370294511599627`).

```
1) Update to v0.24.100 (Updates tab -> Download -> restart). The top-left box should say v0.24.100.
2) Endgame savestates: restore one that has to load the endgame area - a Full load of a state you captured on the surface after being in the endgame, or a Quick load of a state past the vault door on a save where you haven't opened it. Before, the game froze ~5 s; now you should be held in place while it loads (no freeze). How long were you held? Anything missing afterwards (floor, lab, elevators, doors)?
3) Same thing on a slower PC / hard drive if you have one: does it still finish, and does anything look unloaded when you're let go?
4) If the game ever says "can't carry any more <item>" after a restore, press Mark and send a report straight away - the log now says what caused it.
5) Weaker PC? Play normally for ~10 minutes, send a report, and post your CPU / GPU / RAM - it's for the upcoming FPS work.
```

What each checks:
1. The update path to a three-digit patch version (0.24.100 > 0.24.99 is
   tested; this is the real updater).
2. `EndgameAsyncForRestores` (v0.24.99) on other machines: the hold
   (`held the player ... for N s`) and the `Performance: endgame loaded in
   the background ...` line; the endgame intact.
3. The same on slow storage: the load outlasting the 30 s hold (the hold
   gives up at 30 s and says so).
4. `Inventory full: ... - from <call stack>` (v0.24.98) - the plane axe
   lead (RefreshHeld's put-away racing the load's equip).
5. Raw FPS baseline: `Perf (30 s)` lines + specs from a low-end machine.

Not asked (confirmed by bridge on the released builds): the endgame
restores themselves on the author's machine, and a Go mid-ride stopping
the red elevator ride (v0.24.100).
